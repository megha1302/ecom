<%= form_with(model: ordrproduct, local: true) do |form| %>
  <% if ordrproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ordrproduct.errors.count, "error") %> prohibited this ordrproduct from being saved:</h2>

      <ul>
        <% ordrproduct.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :ordr_id %>
    <%= form.text_field :ordr_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
